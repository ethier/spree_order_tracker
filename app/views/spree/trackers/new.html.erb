<h2 class="attic-header">Track my order</h2>
<div class="general-block standard">
	<%= image_tag("trackOrder.jpg") %>
	<%= form_tag("http://localhost:3000/trackers") do %>
        <p>Tracking the status of your GelaSkins order is super easy. Just enter the order number and email address (the one you used to place the order) and you're good to go.</p>
	      <div class="row">

				<div class="span3">
						<%= label_tag(:order_number) %>
						<%= text_field :order_number, nil, :size => 5, :class => 'track_input_order' %>
				</div>

				<div class="span5">
						<%= label_tag(:email) %>
		  				<%= text_field :email, nil, :size => 5, :class => 'track_input' %>
		  		</div>
	  			
	  			<div class="span4">
					<%= submit_tag t('orders_tracker.track!') %>
				</div>

			</div>
	<% end -%>

</div>
